using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class trafficLightController : MonoBehaviour 
{
    /*public GameObject trafficLightObject;  // User-assigned reference to the TrafficLight game object
    private TrafficLight trafficLight; // Reference to the TrafficLight script
    private Renderer renderer;  // Reference to the object's renderer component

    void Start()
    {
        // Get the TrafficLight component from the user-assigned game object
        trafficLight = trafficLightObject.GetComponent<TrafficLight>();

        // Get the renderer component from the user-assigned game object
        renderer = trafficLightObject.GetComponent<Renderer>();
    }

    void Update()
    {
        // Check if trafficLight is not null before accessing its currentState
        if (trafficLight != null)
        {
            // Check the current state of the traffic light
            if (trafficLight.currentState == TrafficLight.LightState.Red)
            {
                // Play red light script
                if (renderer != null)
                {
                    renderer.material.color = Color.red;
                }
                Debug.Log("RED");
            }
            else if (trafficLight.currentState == TrafficLight.LightState.Yellow)
            {
                // Play yellow light script
                if (renderer != null)
                {
                    renderer.material.color = Color.yellow;
                }
                Debug.Log("YELLOW");
            }
            else if (trafficLight.currentState == TrafficLight.LightState.Green)
            {
                // Play green light script
                if (renderer != null)
                {
                    renderer.material.color = Color.green;
                }
                Debug.Log("GREEN");
            }
        }
        else
        {
            Debug.LogError("Traffic light reference not set!");
        }
    }*/
}



